node_modules/
dist/
.vite/

# Alchemy local deploy state (per-stage, never committed).
.alchemy/

# Owned clarification's durable local records (spec #221): the run-record
# store and managed-session storage are host-repo-scoped local state,
# never committed.
.workbench/

# Project-specific configuration belongs to the host repository.
workbench.config.json

# Generated from the host repository's local Markdown corpus.
src/data.generated.ts

# Firecrawl tool session scratch.
.firecrawl/

# Local dotenvx environment files; .env.example and .env.demo stay tracked.
.env
.env.local
.env.*.local
