# Demo data for `pnpm dev:demo` and `pnpm sync:demo`. Tracked, because it
# holds no secrets. Any value — even one naming another checkout path —
# turns demo mode on; sync-data resolves it via demoSourceRoot().
WORKBENCH_DEMO_SOURCE=1
