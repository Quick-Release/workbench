---
"@quick-release/workbench": patch
---

Install the Effect skills (`effect-ts`, `effect-v3-to-v4`) from `Effect-TS/skills` alongside the existing set, so agents working this Effect 4 codebase get the library's own conventions.
