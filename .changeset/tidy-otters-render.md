---
"@quick-release/workbench": minor
---

Session handoffs lens on the Agent sessions page: the sessions collector counts Skill tool calls per session (still reading only the session database), and the new Handoffs lens joins the recorded parent links into trees with per-session skill-call counts and honest gap/cycle caveats — observed only, never acted on. (#65)
